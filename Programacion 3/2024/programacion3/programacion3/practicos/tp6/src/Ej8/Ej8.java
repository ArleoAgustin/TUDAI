package Ej8;

import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;

public class Ej8 {
/*
    private GrafoDirigido grafo;
    private HashMap<Integer, String> vertices;
    private ArrayList coloresUsados;

    public Ej8(GrafoDirigido g) {
        this.grafo = g;

        Iterator<Integer> verticesIterator = grafo.obtenerVertices();

        while (verticesIterator.hasNext()){
            this.vertices.put(verticesIterator.next(), null);
        }

    }

    public void colorear(ArrayList<String> colores){

        Iterator<Integer> verticesIterator = grafo.obtenerVertices();

        while(verticesIterator.hasNext()){

            Vertice v = verticesIterator.next();

            if (v.getColor() == null){

                this.asignarColor(colores, v);
            }

        }

    }

    private void asignarColor(ArrayList<String> colores, Vertice v){

        ArrayList<> coloresDeMisVecinos = v.verColorDeMisVecinos();

        for (String color: colores){

            if (!coloresDeMisVecinos.contains(color) && this.coloresUsados.contains(color)){
                v.pintar(color);
                return;
            }

            if (!coloresDeMisVecinos.contains(color) && !this.coloresUsados.contains(color)){
                v.pintar(color);
                coloresUsados.add(color);
                return;
            }

        }
    }
*/
}
